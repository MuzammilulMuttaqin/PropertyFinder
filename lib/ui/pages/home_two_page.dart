import 'package:flutter/material.dart';
class HomeTwoPage extends StatelessWidget {
  const HomeTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Home two page'),
      ),
    );
  }
}